[project]
name = "trader"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "anthropic>=0.121.0",
    "fastapi>=0.141.1",
    "jinja2>=3.1.6",
    "markdown>=3.10.3",
    "openai>=3.0.0",
    "pydantic-ai>=2.29.0",
    "python-dotenv>=1.2.2",
    "tabulate>=0.10.0",
    "uvicorn>=0.52.2",
]

[tool.hatch.build.targets.wheel]
packages = ["trader"]

[dependency-groups]
dev = [
    "pytest>=9.1.1",
]
